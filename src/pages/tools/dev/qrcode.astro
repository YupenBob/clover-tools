---
import ToolLayout from '../../../layouts/ToolLayout.astro';
import ToolPanel from '../../../components/ToolPanel.astro';
import { TOOLS } from '../../../lib/tools';

const category = 'dev' as const;
const tool = TOOLS.dev.find((t) => t.slug === 'qrcode')!;
const related = TOOLS.dev.filter((t) => t.slug !== tool.slug).slice(0, 6);
---

<ToolLayout category={category} tool={tool} related={related} categoryName="开发实用">
  <ToolPanel title="生成配置" icon="bi-qr-code">
    <div slot="actions">
      <button class="btn btn-ghost btn-sm" id="qrSample" type="button"><i class="bi bi-magic"></i> 示例</button>
    </div>
    <div class="row">
      <div class="field">
        <label class="field-label" for="qrType">类型</label>
        <select class="select" id="qrType">
          <option value="qrcode" selected>二维码</option>
          <option value="barcode">条形码（Code128）</option>
        </select>
      </div>
      <div class="field">
        <label class="field-label" for="qrSize">尺寸</label>
        <select class="select" id="qrSize">
          <option value="256">256px</option>
          <option value="384" selected>384px</option>
          <option value="512">512px</option>
        </select>
      </div>
      <div class="field">
        <label class="field-label" for="qrLevel">容错级别</label>
        <select class="select" id="qrLevel">
          <option value="L">L（7%）</option>
          <option value="M" selected>M（15%）</option>
          <option value="Q">Q（25%）</option>
          <option value="H">H（30%）</option>
        </select>
      </div>
    </div>
    <div class="field mt-3">
      <label class="field-label" for="qrText">内容</label>
      <textarea class="textarea" id="qrText" spellcheck="false" placeholder="输入文本、链接或任意内容"></textarea>
    </div>
    <div class="btn-row">
      <button class="btn btn-primary" id="qrBtn" type="button"><i class="bi bi-qr-code"></i> 生成</button>
      <button class="btn btn-ghost" id="qrDownload" type="button"><i class="bi bi-download"></i> 下载 PNG</button>
    </div>
    <div class="status-msg" id="qrStatus" role="status" aria-live="polite"></div>
  </ToolPanel>

  <ToolPanel title="生成预览" icon="bi-eye">
    <div class="qr-preview">
      <canvas id="qrCanvas"></canvas>
      <svg id="barcodeSvg"></svg>
    </div>
  </ToolPanel>
</ToolLayout>

<style>
  .qr-preview {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-5);
    background: #ffffff;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
  }

  .qr-preview canvas,
  .qr-preview svg {
    max-width: 100%;
  }
</style>

<script>
  import { byId, showStatus, hideStatus, bindSample } from '../../../scripts/toolkit';
  import QRCode from 'qrcode';
  import JsBarcode from 'jsbarcode';

  const canvas = byId<HTMLCanvasElement>('qrCanvas');
  const svg = byId<SVGElement>('barcodeSvg');

  async function generate() {
    hideStatus('qrStatus');
    const text = byId<HTMLTextAreaElement>('qrText').value.trim();
    const type = byId<HTMLSelectElement>('qrType').value;
    const size = Number(byId<HTMLSelectElement>('qrSize').value);
    if (!text) {
      showStatus('qrStatus', 'error', '请输入内容');
      return;
    }
    canvas.style.display = type === 'qrcode' ? '' : 'none';
    svg.style.display = type === 'barcode' ? '' : 'none';
    try {
      if (type === 'qrcode') {
        const level = byId<HTMLSelectElement>('qrLevel').value as 'L' | 'M' | 'Q' | 'H';
        await QRCode.toCanvas(canvas, text, {
          width: size,
          margin: 2,
          errorCorrectionLevel: level,
          color: { dark: '#232019', light: '#ffffff' },
        });
      } else {
        JsBarcode(svg, text, {
          format: 'CODE128',
          width: 2,
          height: 80,
          displayValue: true,
          lineColor: '#232019',
          background: '#ffffff',
        });
      }
      showStatus('qrStatus', 'success', `${type === 'qrcode' ? '二维码' : '条形码'}生成成功`);
    } catch (e) {
      showStatus('qrStatus', 'error', `生成失败：${(e as Error).message}`);
    }
  }

  function triggerDownload(dataUrl: string) {
    const a = document.createElement('a');
    a.href = dataUrl;
    a.download = 'clover-code.png';
    document.body.appendChild(a);
    a.click();
    a.remove();
  }

  byId<HTMLButtonElement>('qrBtn').addEventListener('click', generate);
  byId<HTMLButtonElement>('qrDownload').addEventListener('click', () => {
    const type = byId<HTMLSelectElement>('qrType').value;
    if (type === 'qrcode') {
      triggerDownload(canvas.toDataURL('image/png'));
      return;
    }
    const xml = new XMLSerializer().serializeToString(svg);
    const svgBlob = new Blob([xml], { type: 'image/svg+xml' });
    const img = new Image();
    img.onload = () => {
      const c = document.createElement('canvas');
      c.width = img.width;
      c.height = img.height;
      c.getContext('2d')!.drawImage(img, 0, 0);
      triggerDownload(c.toDataURL('image/png'));
    };
    img.src = URL.createObjectURL(svgBlob);
  });

  bindSample('qrSample', 'qrText', 'https://clovertools.cn');
</script>
